function setup() {
  createCanvas(window.innerWidth, window.innerHeight);
  background(255); let turtle = makeTurtle(width/2, height/2);
// -------------------------------------------------

  
  turtle.setColor('#8247bd')
  turtle.setWeight(10)

  turtle.forward(100)
  
  
  
// -------------------------------------------------  
} function draw() {}